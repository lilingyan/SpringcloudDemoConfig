spring:
  application:
    name: service-feign
  zipkin:
    base-url: http://localhost:9411
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8766

feign:
  hystrix:
    enabled: true